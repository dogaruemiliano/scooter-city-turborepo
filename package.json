{
  "name": "turborepo-full-template-v2",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e",
    "gen": "pnpm gen:env && pnpm --filter @repo/api-shared build && pnpm --filter api build && pnpm gen:openapi && pnpm gen:orval",
    "gen:env": "tsx scripts/generate-env-example.ts",
    "gen:openapi": "pnpm --filter api spec > openapi.json",
    "gen:orval": "orval --config ./orval.config.ts || true",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "lefthook": "^1.8.4",
    "lint-staged": "^15.2.10",
    "orval": "^7.3.0",
    "prettier": "^3.7.4",
    "tsx": "^4.19.2",
    "turbo": "^2.9.12",
    "typescript": "5.9.2",
    "zod": "^3.23.8"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  },
  "pnpm": {
    "overrides": {
      "esbuild": "^0.27.7",
      "jest-mock": "^30.0.0"
    }
  }
}
