# `@repo/eslint-config`

Shared ESLint flat configurations for the workspace.

Use this package from app/package `eslint.config.mjs` files instead of copying
rules between packages. Keep broad workspace rules here and app-specific rules
inside the app that needs them.
